local lib_name = "NektoExp"
local lib_main_name = "Conhosted"
local lib_main_name_f9 = "[Conhosted]"
local lib_name_f9 = "[NektoExp]"
local lib_version = "1.0.3"
local lib_status = "Release"
local frametech = "eblanoid"

if not isfolder("NektoLib") then
	makefolder("NektoLib")
end

if not isfolder("NektoLib/name") then
	makefolder("NektoLib/name")
end

if not isfile("NektoLib/vsuid.txt") then
	writefile("NektoLib/vsuid.txt", "qwa121sasz")
end

if not isfile("NektoLib/name/lib.txt") then
	writefile("NektoLib/name/lib.txt", "Conhosted")
end

print(lib_name_f9 .. " Loading library...")
task.wait(2)

print(lib_name_f9 .. " Conhosted powered by NektoLib")
task.wait(1)

local Players = game:GetService("Players")
local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "Conhosted"
gui.IgnoreGuiInset = true
gui.DisplayOrder = 9999999
gui.Parent = game.CoreGui

local frame = Instance.new("Frame")
frame.Name = "Main"
frame.Parent = gui
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.Position = UDim2.fromScale(0.5, 0.535)
frame.Size = UDim2.fromScale(0.214, 0.15)
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BorderSizePixel = 0

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = frame

local title = Instance.new("TextLabel")
title.Name = "Title"
title.Parent = frame
title.Position = UDim2.fromScale(0.045, 0)
title.Size = UDim2.fromScale(0.9, 0.2)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.SourceSans
title.Text = lib_main_name .. " v" .. lib_version
title.TextScaled = true
title.TextXAlignment = Enum.TextXAlignment.Left

local status = Instance.new("TextLabel")
status.Name = "Status"
status.Parent = frame
status.AnchorPoint = Vector2.new(0.5, 0.5)
status.Position = UDim2.fromScale(0.5, 0.55)
status.Size = UDim2.fromScale(0.9, 0.3)
status.BackgroundTransparency = 1
status.TextColor3 = Color3.fromRGB(255, 255, 255)
status.Font = Enum.Font.SourceSans
status.TextScaled = true
status.Text = "Loading..."

local states = {
	"Loading",
	"Loading.",
	"Loading..",
	"Loading..."
}

for i = 1, 3 do
	for _, text in ipairs(states) do
		status.Text = text
		task.wait(0.25)
	end
end

for _, text in ipairs({
	"Setup",
	"Setup.",
	"Setup..",
	"Setup..."
	}) do
	status.Text = text
	task.wait(0.25)
end

status.Text = "Loaded! NektoHub running in 2-3 seconds"

task.wait(3)
warn("Library integrated!. ID: " .. frametech)
gui:Destroy()
